1）播放常见的视频格式，包括但不限于avi、mkv、mp4(3种编码格式)、flv等；
（完成）:测试用例包括avi和mp4格式
2）视频文件支持存储于本地（例如D:\1.mp4）、局域网Samba服务器(\\192.168.1.2\1.mp4)、Http服务器(http://192.168.1.2/1.mp4)、media服务器。
（完成）：测试用例包括本地和Http部分

3）视频支持跳转，提供外部跳转接口。（帧/时间）
（完成）
4）支持A->B点循环播放的功能（如1分10秒到1分25秒内循环播放）。
（完成50%）可能存在BUG后面完善
5）基本的视频控制功能支持：暂停、播放、音量+、音量-、全屏切换、播放速度控制（至少支持0.5x、2x、4x、8x）、画面旋转（支持90°、180°、270°）；
（完成）
6）视频局部放大功能；（提供接口，允许用户手动画框后弹出新的播放层，播放框内的视频数据）。
（完成）：需要确定放大显示的方式
7）提供截屏接口。
（完成）
8）控件拥有滚动条功能，支持拖拽。
（完成）
9）播放图片集合功能（List<string> ImageUrls），将集合内的图片按顺序反复播放，默认间隔为0.5秒。
（未完成）：后续迭代
10）播放图片文件夹功能，按照文件名按名称排序正序播放，默认间隔为0.5秒。
（未完成）：后续迭代
11）提供接口返回当前播放的Url，文件夹时返回文件Url。
（完成50%）
12）提供接口返回当前的帧数与总帧数。
（完成）
13）界面美观。提供原型确认。
（未完成）：后续迭代
14）若选用第三方播放器内核，需暴露视频字幕接口及相关功能。若自行开发，该条省略。
（未完成）：后续迭代

